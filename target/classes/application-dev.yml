spring:
  datasource:
    driver-class-name: com.mysql.cj.jdbc.Driver
    url: jdbc:mysql://localhost:3306/boke?useUnicode=true&characterEncoding=utf-8&serverTimezone=UTC
    username: root
    password: password
  jpa:
    hibernate:
      ddl-auto: update #实体类进行更改以后，自动更新表结构
    show-sql: true

    properties:
      hibernate:
        dialect: org.hibernate.dialect.MySQL5Dialect #方言

  level:
    root: info
    com.jjy: debug
  file:
    name: log/blog.log










